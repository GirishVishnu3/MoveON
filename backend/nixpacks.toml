providers = ["python"]

[variables]
PYTHON_VERSION = "3.11"

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python create_db.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
